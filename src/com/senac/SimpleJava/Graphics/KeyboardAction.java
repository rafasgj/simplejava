package com.senac.SimpleJava.Graphics;

/**
 * Interface to implement object that will respond to keyboard events.
 */
public interface KeyboardAction {
	/**
	 * When the specific keyboard event occurs, this method is called.
	 */
	void handleEvent();
}
